{
  "name": "image-resize",
  "version": "1.0.0",
  "description": "Serverless image resizing",
  "readme": "Serverless image resizing",
  "repository": "https://github.com/citizensadvice/serverless-image-resize",
  "license": "Apache-2.0",
  "main": "index.js",
  "dependencies": {
    "color": "3.1.2",
    "color-name": "1.1.4",
    "sharp": "^0.26.0"
  },
  "devDependencies": {
    "aws-sdk": "2.631.0",
    "aws-sdk-mock": "^4.4.0",
    "jest": "^24.7.1"
  },
  "scripts": {
    "test": "jest",
    "build:init": "rm -rf .aws-sam/ dist/ node_modules/",
    "build": "npm run build:init && npm install --production --arch=x64 --platform=linux",
    "build:debug": "npm run build:init && npm install --arch=x64 --platform=linux",
    "debug": "sam local invoke ImageResizerHandlerFunction --event events/event-s3.json --parameter-overrides AppBucketName=test-bucket"
  },
  "jest": {
    "testPathIgnorePatterns": [
      ".aws-sam/"
    ]
  }
}
