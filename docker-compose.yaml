
version: "3"

services:
  app:
    build: .
    image: "citizensadvice/image-resizer"
    ports:
      - 4567:4567
    volumes:
      - .:/app
      - ./spec/fixtures/image_files:/app/image_files
    environment:
      - URL_HOST=app.test:4567
      - MOKTA_ISSUER=https://cadev.oktapreview.com